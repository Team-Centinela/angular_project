# API base URL placeholder.
# Copy to frontend/.env (gitignored) and adjust as needed.
# Currently informational — the frontend reads from
# frontend/src/environments/environment*.ts (selected by fileReplacements
# in angular.json). Wiring .env into ng serve is tracked as a follow-up.
API_BASE_URL=http://localhost:3000
